HTML-элементы — основа языка HTML.
Каждый HTML-документ 
состоит из дерева HTML-элементов и текста. 
Каждый HTML-элемент 
обозначается
начальным (открывающим) и конечным 
(закрывающим) тегом. 
Открывающий и закрывающий
теги содержат имя элемента.
<a> — гиперссылка.

<img> — изображение.

<button> — кнопка.

<input> — поле ввода.

<table> — таблица.
<tr> — строка таблицы.
<th> — заголовочная ячейка.
<td> — ячейка данных.

<ul> — ненумерованный список.
<ol> — нумерованный список.
<li> — элемент списка.

<h1>–<h6> — заголовки разного уровня.
<p> — абзац.
<br> — перенос строки.
<span> — inline-элемент для стилей.
